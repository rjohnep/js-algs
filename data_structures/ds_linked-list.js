const makeNode = () => {};

const makeLinkedList = () => {};

const linkedList = makeLinkedList();

assert.strictEqual(linkedList.isEmpty(), true);

linkedList.push(1);
linkedList.push(2);
linkedList.push(3);
